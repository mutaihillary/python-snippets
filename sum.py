#Create a function that gets the sum of all the digits in a list given as a parameter
#pseudo code
#define a function
#get the values
#add
#print the sum

numbers = [1, 2, 3]
numsum = (sum(numbers))
print(numsum)